[project]
name = "chgsmart"
version = "0.1.0"
description = "Smart changelog generator"
authors = [{ name="Olivia Vasquez" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "gitpython",
    "click",
]

[project.scripts]
chgsmart = "chgsmart.__main__:main"